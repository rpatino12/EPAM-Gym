server:
  port: 8081

spring:
  application:
    name: trainer-service
  output:
    ansi:
      enabled: ALWAYS
  h2:
    console:
      enabled: 'true'
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: